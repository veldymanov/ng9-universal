import { AppNoShellRenderDirective } from './app-no-shell-render.directive';

describe('AppNoShellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNoShellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
